<h2>{{localize "HEIST.JackSheet.Reconnaissance.Title"}}</h2>

<p>{{localize "HEIST.JackSheet.Reconnaissance.Explanations"}}</p>

<div class="grid grid-5col align-center">
  <div>
    <span class="badge">
      <span>{{localize "HEIST.Cards.Drawn"}}</span>
      <strong class="badge-value">{{length reconnaissanceHand.availableCards}}</strong>
    </span>
  </div>

  {{#each colors}}
    <div>
      <span class="badge">
        <span class="card-suit-{{@key}}">{{this.icon}}</span>
        <span>{{this.label}}</span>
        <strong class="badge-value{{#if this.isOverflowed}} overflowed{{/if}}">{{this.value}}</strong>
      </span>
    </div>
  {{/each}}
</div>

{{#if agentsCompromised}}
  <h1 class="grid-span-5 compromised align-center">{{localize "HEIST.JackSheet.Reconnaissance.AgentsCompromised"}}</h1>
{{/if}}

{{#if (and canDrawReconnaissance (not agentsCompromised))}}
  <div class="flexrow">
    <button type="button" data-draw>{{localize "HEIST.Global.DrawCards"}}</button>
  </div>
{{/if}}

<div class="grid grid-5col align-center cards-container">
  {{#each reconnaissanceHand.cards}}
    <div>
      <img src="{{this.img}}" alt="{{this.name}}" class="card" />
    </div>
  {{/each}}
</div>
