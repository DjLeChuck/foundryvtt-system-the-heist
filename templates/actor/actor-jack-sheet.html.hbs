<form class="{{cssClass}}" autocomplete="off">
  {{#if editable}}
    <p class="sheet-edit">
      <button type="button" class="action-btn" data-tooltip="{{localize "HEIST.Global.Edit"}}" data-lock>
        <i class="fa {{ifThen isLocked "fa-lock" "fa-unlock"}}"></i>
      </button>
    </p>
  {{/if}}

  <div class="jack-wrapper flexcol">
    <div class="jack-image">
      <img src="{{actor.img}}"{{#if (and editable (not isLocked))}} data-edit="img"{{/if}} alt="" />
    </div>

    <div class="flexshrink">
      {{#if (and editable (not isLocked))}}
        <input name="name" type="text" value="{{actor.name}}" placeholder="{{localize "HEIST.Global.Name"}}" />
      {{/if}}
      {{#if (or (not editable) isLocked)}}
        <p>{{actor.name}}</p>
      {{/if}}
    </div>
  </div>
</form>
